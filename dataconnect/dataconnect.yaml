specVersion: "v1"
serviceId: "deepen-qt"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "deepen-qt-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
